/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/
# The package tarball mix hex.build writes.
hue-*.tar

# The Livebook example's saved pairing (an application key is a password).
/examples/.hue.json
